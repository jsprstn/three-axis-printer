void letterA(void)
{
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterB(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterC(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterD(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterE(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterF(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterG(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterH(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call  zLift(-1);
  // Lowers the printer tip

  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterI(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterJ(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterK(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterL(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterM(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterN(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterO(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterP(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterQ(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1); // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterR(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterS(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterT(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterU(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterV(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterW(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterX(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1); // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterY(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void letterZ(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1);  // Lowers the printer tip
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}

void space(void)
{
  // UNFINISHED DUE TO FAILED STEPPER BOARD
  // Performs the combination of x and y moves
  // to print the letter given in the function call

  zLift(-1); // Lowers the printer tipa
  xMove(20, 1, 0); // Moves 20 cycles in the X-up direction
  yMove(20, 1, 0); // Moves 20 cycles in the Y-up direction
  xMove(20, -1, 0); // Moves 20 cycles in the X-down direction
  xMove(20, -1, 1); // Moves 20 cycles in the Y-up direction, raises the printer tip
}
